[project]
name = "deep-ml"
version = "0.1.0"
description = "Solutions repo to Deep-ML exercises."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "scikit-learn>=1.7.2",
]
